version: 0.0
os: linux
files:
  - source: /
    destination: /apps/vc-chat
hooks:
  BeforeInstall:
    - location: deploy/before_install
      timeout: 300
  AfterInstall:
    - location: deploy/after_install
      timeout: 300
runas: ec2-user
